/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
        remotePatterns: [{
            protocol: "https",
            hostname: "rr3---sn-npoe7nez.c.drive.google.com",
        }, ],
    },
};

export default nextConfig;